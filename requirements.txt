# Python 3.13 — create venv then: pip install -r requirements.txt
# (xarray/netCDF4 are optional: only needed if you switch to CEDA NetCDF
# downloads, which require a CEDA account; the pipeline as shipped does not.)
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.13.0
pillow==12.3.0
matplotlib==3.11.1
folium==0.20.0
branca==0.8.2
